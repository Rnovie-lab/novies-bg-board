# BG Board — Handoff for next session
*End of session 2026-05-19. Read this first.*

## Where we are right now

BG Board's been live on `bgboard.up.railway.app` since yesterday. Today we shipped:
- Save persistence on Railway (Volume + env var) — verified working
- Parser handles 9 more date formats AND now picks up dates from sequential-layout PDFs (St. Denis-style schedules) that previously dropped them
- Inline date editor on every day in Breakdown (so testers can fix dates the parser misses)
- "Project Totals" strip on Breakdown / Export / PDF with a top↕bottom toggle that persists per project
- Download PDF button moved to the upper-right of the Export header

## What's on Ross's disk but NOT pushed yet (Phase 2.1 rework)

The dashboard-first rework Ross approved after several local-review passes. Tested in localhost, working, just needs to be pushed.

**Changes:**
- New **Projects dashboard** is the default landing page (card grid; click a card to open; trash icon bottom-left)
- **📂 Projects became a 4th nav tab** alongside Setup / Breakdown / Export, tabs centered in header
- **BETA pill** next to the logo
- **"+ New Project"** button in header (left of "⚑ Report Issue")
- **Save and Delete buttons removed** — replaced by autosave (debounced 1.5s after any change, with a small "✓ Saved" indicator upper-right)
- **Import Shooting Schedule button moved** from the global header into the lower-center of Setup → Show Information. After import you auto-jump to Breakdown.
- **Breakdown empty state** no longer has the Import button — just text pointing back to Setup.

**Critical bug fixes during local testing (already in the same commit):**
- `defaultState()` was missing `defaultRates` → broke renderSetup whenever state was reset. Fixed.
- `loadSave` now migrates `defaultRates` for older saves.
- `importPDF` was crashing on now-removed `pdf-import-btn` / `pdf-import-label` DOM elements. Null-guarded.

**Parser fix:** Sequential-layout PDFs put the day-of-week in the left column and the rest of the date past x=310. Old fallback only fired on empty `date_text`, but "Monday," was truthy-but-useless. Now the fallback also fires when `parse_date()` returns empty. Verified on `217_shootsked_white.pdf` and `Margo_Vegas_Bingo3SS.pdf`.

## First thing to do tomorrow

Push the Phase 2.1 changes (sandbox can't unlink `.git` locks, so this is on Ross):

```
cd "/Volumes/Envoy Pro/Claude/Projects/BG Board"
rm -f .git/index.lock .git/HEAD.lock
git add -A
git commit -m "feat(phase2.1): dashboard rework + parser sequential-layout date fix"
git push
```

After Railway redeploys, have a beta tester import a real PDF and confirm dates flow through to day headers.

## Open items still owed

1. **Two-browser auth verification** — owed since 2026-05-18 handoff. Ross signs in, opens incognito, signs up as a fake second tester, confirms zero workspace overlap. ~10 min on his side.
2. **Beta-tester comms blurb** — Ross drafted it; unclear if sent.
3. **CSC parser-coordination reply** — drafted; unclear if sent.

## Phase 2.2 — next chunk of rework

Autosave already shipped as part of 2.1, so 2.2 is smaller than originally scoped.

- **Soft gate empty Breakdown** with a more obvious nudge back to Setup ("← Go to Setup" button instead of just text)
- **Verify autosave doesn't thundering-herd** under rapid edits

## Phase 2.3 — cleanup phase (after 2.2 settles)

- **Auto-rename "Untitled" projects** using show name + episode (only when both are populated — Ross confirmed this nuance)
- **Help "?" button** + quick-start modal (Ross writes the copy)
- **Share button** in header (matches CSC pattern)
- **One-time "what's new" banner** on dashboard
- **Remove dead code** orphaned by 2.1: `openSavesPanel`, `openSaveAs`, `doSave`, `resetAll`, `startBlankBreakdown`, and the dead saves modal HTML

## Workflow rules I should follow

- **Review locally before pushing rework changes.** For the Phase 2 work, commit locally and let Ross test in localhost first. Don't push until he approves.
- **Lock file dance.** Sandbox can't unlink `.git/index.lock` or `.git/HEAD.lock`. Ross runs `rm -f .git/index.lock .git/HEAD.lock` from his Terminal whenever I hit the lock.
- **Less jargon.** Lead with concepts. Skip line numbers, function names, naming decisions unless he asks. He shut me down on heavy technical framing earlier today.
- **Make engineering calls myself.** Only kick non-engineering decisions (sequencing, beta-tester lists) to Ross, one at a time. Learned this the hard way during the Clerk integration.

## Decisions already locked in for the rework

- New Project → Setup with project name as the FIRST field (no upfront modal)
- "← Projects" back-nav handled by the existing Projects nav tab (not a separate link)
- Help modal: quick-start bullets, no external link
- Legacy "Untitled" projects: auto-rename only when show name + episode are populated
- Autosave: debounced ~1.5s after last change
- Setup → Breakdown: soft gate (Breakdown reachable, empty state nudges back)

## Tone

Ross is a filmmaker with deep production knowledge and a love of AI/tech. Wants the firm-friendly approach. Push back on weak ideas with evidence. He shipped a lot today — likely tired tomorrow until coffee.
